// Здесь будет что-то вроде этого

// export async function createFlashcard(input: CreateFlashcardInput) {
//   validate(input);
//   const card = await repo.create(input);
//   await analytics.track('flashcard_created');
//
//   return card;
// }
